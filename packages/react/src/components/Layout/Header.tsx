import Logo from '@/components/Logo';
import NavBar from '@/components/Navbar';

export default function AppHeader() {
  return (
    <header className='container  self-start mx-auto min-h-16'>
      <NavBar />
    </header>
  );
}
